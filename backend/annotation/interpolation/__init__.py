"""Phase 8 — interpolation.

``core`` is pure mathematics: numpy in, numpy out, no Django, no database, no
filesystem. ``service`` is the orchestration that loads labels, calls the core,
writes the result and records a Phase 7 operation.
"""
