# v1.1.0 production transition validation

Validation date: 2026-08-01. This record distinguishes implementation,
configuration, staging evidence, and public-production evidence. A green
feature flag is never treated as sufficient evidence by itself.

## Release candidate

- Branch: `release/mito-data-studio-v1.1.0`
- Integrated baseline: `5276e23` (`feat: integrate coherent webknossos upgrade profile`)
- Candidate commit: the commit containing this final pre-cutover evidence;
  the annotated tag resolves it without relying on a mutable branch name
- Profile: `production_integrated_v1`
- Public predecessor: `a9865fd829c768ef7a6e68613cec08c6e96827af`
- Public successor/tag: filled only after every pre-cutover gate passes

## Automated gates

| Gate | Result | Evidence |
|---|---|---|
| PostgreSQL backend default feature matrix | pass | initial 1,258/1,258 in 1,302.351 s; detached clean release 1,258/1,258 in 1,129.917 s |
| Production contract/data-root focused backend suite | pass | 47/47 in 25.813 s |
| Django system check | pass | zero issues |
| Django deploy check | pass with documented warnings | test-secret warning plus intentionally disabled HSTS includeSubDomains/preload; production uses a generated strong secret |
| `makemigrations --check --dry-run` | pass | no changes detected |
| Empty PostgreSQL migration | pass | all migrations through `annotation.0013` and `volumes.0006`; subsequent plan empty |
| Restored-production PostgreSQL migration | pass | 4 legacy tasks reconciled to 4 instances; strict readiness has zero blockers |
| Frontend TypeScript | pass | `tsc --noEmit` |
| Frontend unit/component tests | pass | 157/157 |
| Production frontend build | pass | profile embedded; PullQueue and renderer false; no demo credentials |
| Chromium Phase 14 | pass | 5/5; memory growth 0.715%; XY/XZ/YZ and viewer path covered |
| Dependency licence inventory | pass | Python 57, JavaScript 181, missing evidence 0, AGPL dependencies 0 |
| Git/LFS integrity | pass | three model objects present as bytes; `git lfs fsck` OK |
| Shell syntax | pass | every release/staging shell script passes `bash -n`; shellcheck is unavailable on this host and is not claimed |
| Detached clean release checkout | pass | independent CPython 3.11.14 venv; hash-pinned pip install; `npm ci`; LFS OIDs/bytes verified; tree clean |
| Empty production-profile database | pass | checks green, deploy check has only intentional HSTS subdomain/preload warnings, migration plan applied fully, subsequent plan empty, strict readiness green |
| Frontend production dependency audit | accepted exception | exact React Router 7.18.2; registry reports GHSA-qwww-vcr4-c8h2, which upstream says only affects unused unstable RSC APIs; no RSC/SSR/actions in this Vite SPA |

An exploratory run incorrectly forced `production_integrated_v1` over the
entire historical feature-off matrix and finished 15 failures / 13 errors out
of 1,258. The failures were the expected contradictions (for example tests
which require autosave/review/operations to be disabled). It is retained as a
failed-method result, not counted as a gate. The supported split is the full
default/override matrix above plus the focused production contract checks and
real production-profile staging/browser gates.

## Production-data staging

- Clean checkout, Python 3.11 venv, dedicated PostgreSQL database, dedicated
  data root, three-worker Gunicorn and private TLS ingress were used.
- Empty and restored-data migrations passed without in-place production edits.
- Region-mask schema reconciliation is deterministic and reports exact
  compatibility without changing data.
- Restored Projects, People and Hard Cases rendered; anonymous sharing was
  read-only; TIFF XY/XZ/YZ was used without chunk requests.
- Two authenticated users saved different tasks and reloaded them. Same-base
  concurrent writes produced one 200 and one controlled 409. Cross-task access
  returned 403.
- EfficientSAM and SAM2 both returned compatible working-mask output in a real
  browser flow; source/reference hashes stayed unchanged.
- Integrated organization/team/claim/scheduler/review/region gate passed twice
  with isolated pending tasks (14.4 s and 16.9 s), including Django admin
  session/CSRF behavior through private TLS.
- Final two-user 7,200-second soak passed: 6,955 cycles, 134,357 HTTP 200,
  436 HTTP 304, zero workflow failures, request latency p95 313.55 ms and p99
  447.31 ms, render latency p95 1,782.65 ms, heap envelopes -5.94%/-3.35%,
  maximum heaps 55.98/94.65 MiB, and zero DOM-node envelope growth.
- Midpoint worker reload passed at `2026-08-02T01:15:50Z`: master PID stayed
  `4085417`, three workers were replaced, readiness remained 200, and systemd
  restart count remained zero. Post-reload workflows passed 7/7.
- Access-log classification: zero 5xx and zero unexpected 401. Chromium emitted
  53 credential-free, read-only slice probes; Django rejected every one with
  401 and every exact URI had a paired authenticated 200. The classifier does
  not permit mutation routes, arbitrary endpoints, malformed coordinates, or
  unpaired 401 responses.
- Evidence: `/home/weidf/mito-release-evidence/v1.1.0-soak-comprehensive-rerun02-20260802T002000Z`.

## Private and public production

These sections remain intentionally incomplete until the corresponding gates
actually run.

- Isolated v1.1 production identity/write-target proof: pending.
- Final backup and freeze duration: pending.
- Public HTTPS/cookie/CSRF/identity workflow proof: pending.
- Public Save/reload and old-target-unchanged proof: pending.
- At-least-60-minute observation: pending.
- Cleanup inventory/quarantine: pending.

The transition must not be declared complete while any item in this section is
pending.
