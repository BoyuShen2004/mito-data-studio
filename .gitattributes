# Git LFS — vendor model weights (GitHub rejects files >100MB without LFS).
vendor/sam2/checkpoints/*.pt filter=lfs diff=lfs merge=lfs -text
vendor/efficient_sam/*.onnx filter=lfs diff=lfs merge=lfs -text
