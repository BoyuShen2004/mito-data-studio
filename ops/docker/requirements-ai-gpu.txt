# Core + the AI-assist stack, CUDA 12.4 builds.
#
# Matches the pins in ../../requirements-release.in — the combination the
# release environment was validated against. Requires an NVIDIA driver on the
# host and the NVIDIA Container Toolkit; see the `gpu` profile in
# docker-compose.yml and the GPU section of docs/docker.md.
#
# Expect a large image (roughly 7-8 GB): the CUDA torch wheels alone are ~2.5 GB
# and pull further nvidia-* runtime wheels.

-r requirements-core.txt

--extra-index-url https://download.pytorch.org/whl/cu124

torch==2.5.1+cu124
torchvision==0.20.1+cu124

# CUDA build; it also carries the CPU execution provider, so this single
# package covers GPU and fallback. Never install it alongside plain
# onnxruntime — they provide the same module.
onnxruntime-gpu==1.26.0

# SAM2 helpers.
hydra-core==1.3.4
iopath==0.1.10
tqdm==4.69.1
